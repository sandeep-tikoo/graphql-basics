
const myAdd = (one,two) =>    {
    return one + two
}

// or
// const myAdd = (one,two) =>  one + two          - Short syntax

const mySubtract = (one, two) =>  one - two

export {myAdd as default, mySubtract}